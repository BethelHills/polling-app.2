# Supabase Configuration
# =====================

# Public Supabase URL (safe to expose to client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Public Supabase Anonymous Key (safe to expose to client)
# This key has limited permissions and is safe for client-side use
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key (NEVER commit this to repository!)
# This key has full database access and should only be used server-side
# Store this in GitHub Secrets for CI/CD and local .env.local for development
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Development/Testing
# ===================
NODE_ENV=development

# Optional: Custom Supabase URL for testing
# NEXT_PUBLIC_SUPABASE_URL=https://your-test-project.supabase.co

# Security Notes:
# ===============
# 1. NEXT_PUBLIC_* variables are exposed to the browser
# 2. Never put service role keys in NEXT_PUBLIC_* variables
# 3. Use GitHub Secrets for CI/CD environments
# 4. Keep .env.local in .gitignore
# 5. Use different keys for development, staging, and production
